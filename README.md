## User Authentication Backend 

This project is a simple user authentication backend built with Python and SQLite. It allows users to register and log in, stores passwords securely using hashing and salting, and uses SQL queries to ensure usernames are unique and credentials are validated correctly.
